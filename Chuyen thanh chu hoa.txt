#include<stdio.h>

int main() {
    char s[100];
    int i = 0;
    scanf("%[^\n]", s);
	while( s[i] != '\0' ) {
		if( s[i] >= 'a' && s[i] <= 'z' ) {
	        s[i] = s[i] - 32;
		} 
		i++;
	}
	fflush(stdin);
    puts(s);
    return 0;
}