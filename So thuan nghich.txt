

#include<stdio.h>
#include<math.h>

int main() {
    int N;
    scanf("%d", &N);
    while(N--) {
    	long long a;
         scanf("%lld", &a);
    long long b=a, sum=0;
    while(b!=0){
        sum = sum*10 + b%10;
        b/=10;
    }
        if(sum == a) {
            printf("YES\n");
        } else {
            printf("NO\n");
        }        
    }
    return 0;
}