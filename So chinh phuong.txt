#include<stdio.h>
#include<math.h>

int main() {
    int N;
    scanf("%d", &N);
    while(N--) {
        int a;
        scanf("%d", &a);
        float b = sqrt(a);
        if(b == (int)b) {
            printf("YES\n");
        } else {
            printf("NO\n");
        }        
    }
    return 0;
}